name: Explanation Visual Audit

on:
  pull_request:
    paths:
      - 'explanation-*.js'
      - 'component-contract.js'
      - 'grammatical-components.js'
      - 'derivational-components.js'
      - 'index.html'
      - 'style.css'
      - 'explanation-ui.css'
      - 'tests/visual/**'
      - 'playwright.config.js'
      - 'package.json'
  workflow_dispatch:

jobs:
  section01-explanation-audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Run Section 01 explanation audit
        run: npm run test:visual

      - name: Upload generated screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: section01-explanation-screenshots
          path: test-results/**/section01-explanations/*.png
          if-no-files-found: warn
          retention-days: 7
